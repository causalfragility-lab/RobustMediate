% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline_ipw.R
\name{.fit_gps_model}
\alias{.fit_gps_model}
\title{Fit a natural-spline treatment model and return the GPS model object}
\usage{
.fit_gps_model(treatment_formula, data, df = 4, family = stats::gaussian())
}
\arguments{
\item{treatment_formula}{Original treatment formula (e.g., \code{X ~ Z1 + Z2}).}

\item{data}{Data frame.}

\item{df}{Degrees of freedom for \code{\link[splines:ns]{splines::ns()}}. Default 4.}

\item{family}{GLM family. Default \code{gaussian()}.}
}
\value{
A fitted \code{glm} object with a spline basis for the intercept term
replaced by \code{ns(X, df)} on the \emph{response} side — i.e. the model
\code{X_spline ~ Z} where \code{X_spline} is the ns-expanded treatment.
In practice we use the approach: model X | Z with a Gaussian GLM using
the raw X, then use the \strong{residual SD} from that model to form GPS
densities. The spline enters on the \emph{covariate} side of mediator/outcome
models for the dose-response curve.
}
\description{
Fit a natural-spline treatment model and return the GPS model object
}
\keyword{internal}
