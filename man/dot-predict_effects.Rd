% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline_ipw.R
\name{.predict_effects}
\alias{.predict_effects}
\title{Predict mean potential outcome under a dose pair (x, x_ref)}
\usage{
.predict_effects(models, data, x, x_ref, treat_var, med_var, out_var)
}
\arguments{
\item{models}{List with elements \code{treatment}, \code{mediator}, \code{outcome}.}

\item{data}{Original data frame.}

\item{x}{Dose value (the "treated" level).}

\item{x_ref}{Reference dose (counterfactual baseline).}

\item{treat_var}{Character. Name of treatment variable.}

\item{med_var}{Character. Name of mediator variable.}

\item{out_var}{Character. Name of outcome variable.}
}
\value{
Named numeric vector: \code{NDE}, \code{NIE}, \code{TE}.
}
\description{
Implements the g-computation identification formula for the NDE and NIE:

\deqn{NDE(x, x^*) = E[Y(x, M(x^*))] - E[Y(x^*, M(x^*))]}
\deqn{NIE(x, x^*) = E[Y(x, M(x))]   - E[Y(x,  M(x^*))]}

The mediator is predicted at the \strong{individual} level (not as a scalar mean),
correctly marginalising over each person's covariate values.
}
\keyword{internal}
