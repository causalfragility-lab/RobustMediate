% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meditcv.R
\name{sensitivity_meditcv}
\alias{sensitivity_meditcv}
\title{Mediation ITCV (medITCV) for pathway-specific robustness}
\usage{
sensitivity_meditcv(x, alpha = 0.05)
}
\arguments{
\item{x}{A \code{robmedfit} object returned by \code{\link[=robustmediate]{robustmediate()}}.}

\item{alpha}{Significance level. Default is \code{0.05}.}
}
\value{
An object of class \code{"meditcv"}: a named list with elements
\code{a_path}, \code{b_path}, \code{indirect}, and \code{alpha}. Each pathway element
contains the observed partial correlation, critical partial correlation,
medITCV value, and benchmark confounder impacts.
}
\description{
Computes a mediation-specific extension of Kenneth Frank's (2000)
Impact Threshold for a Confounding Variable (ITCV) for both pathways of a
mediation model:
\itemize{
\item \strong{a-path}: treatment -> mediator
\item \strong{b-path}: mediator -> outcome (controlling for treatment)
}

The mediation ITCV (medITCV) quantifies how strong an unmeasured confounder
would need to be, in terms of the product \eqn{r_{XC} \cdot r_{YC}}, to
invalidate inference for each pathway.
}
\examples{
\donttest{
data(sim_mediation)
  fit <- robustmediate(
    X ~ Z1 + Z2, M ~ X + Z1 + Z2, Y ~ X + M + Z1 + Z2,
    data = sim_mediation, R = 20, verbose = FALSE
  )
med <- sensitivity_meditcv(fit)
print(med)
plot_meditcv(med)
}

}
\references{
Frank, K. A. (2000). Impact of a confounding variable on a regression
coefficient. \emph{Sociological Methods & Research}, 29(2), 147--194.
}
\seealso{
\code{plot_meditcv()}, \code{print.meditcv()}
}
