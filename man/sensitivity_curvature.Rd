% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meditcv.R
\name{sensitivity_curvature}
\alias{sensitivity_curvature}
\title{Dose-Varying Fragility: Curvature-Based Sensitivity}
\usage{
sensitivity_curvature(x, estimand = c("NIE", "NDE", "TE"))
}
\arguments{
\item{x}{A \code{robmedfit} object.}

\item{estimand}{Which estimand to analyse: \code{"NIE"} (default), \code{"NDE"},
or \code{"TE"}.}
}
\value{
A data frame with columns \code{dose}, \code{estimate}, \code{lower}, \code{upper},
\code{se_approx}, \code{frag_local}, \code{curvature}, and \code{in_fragility_zone}.
}
\description{
Computes the fragility curvature of the mediation effect across the full
treatment dose grid. Returns local fragility index, numerical curvature of
the effect curve, and a fragility zone flag at each dose value.
}
\examples{
\donttest{
data(sim_mediation)
  fit <- robustmediate(
    X ~ Z1 + Z2, M ~ X + Z1 + Z2, Y ~ X + M + Z1 + Z2,
    data = sim_mediation, R = 20, verbose = FALSE
  )
curv <- sensitivity_curvature(fit, estimand = "NIE")
plot_curvature(curv)
}

}
\seealso{
\code{plot_curvature()}, \code{sensitivity_meditcv_profile()}
}
