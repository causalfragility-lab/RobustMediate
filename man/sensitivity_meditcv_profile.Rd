% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meditcv.R
\name{sensitivity_meditcv_profile}
\alias{sensitivity_meditcv_profile}
\title{medITCV Robustness Profile: Pathway-Specific Fragility Framework}
\usage{
sensitivity_meditcv_profile(
  x,
  alpha = 0.05,
  delta_grid = seq(0, 0.5, by = 0.01)
)
}
\arguments{
\item{x}{A \code{robmedfit} object.}

\item{alpha}{Significance level. Default \code{0.05}.}

\item{delta_grid}{Numeric vector of confounding impact values over which the
robustness profile is evaluated. Default \code{seq(0, 0.5, by = 0.01)}.}
}
\value{
An object of class \code{"meditcv_profile"}: a named list with elements
\code{a_path}, \code{b_path}, \code{meditcv_indirect}, \code{bottleneck},
\code{robustness_profile}, \code{fragility_ratio}, \code{meditcv_detail}, and \code{alpha}.
}
\description{
Implements the medITCV robustness profile, a formal extension of Frank's
ITCV to causal mediation. Computes pathway-specific fragility thresholds,
applies the minimum robustness principle, and identifies the bottleneck
pathway that governs indirect-effect fragility.
}
\examples{
\donttest{
data(sim_mediation)
  fit <- robustmediate(
    X ~ Z1 + Z2, M ~ X + Z1 + Z2, Y ~ X + M + Z1 + Z2,
    data = sim_mediation, R = 20, verbose = FALSE
  )
mp  <- sensitivity_meditcv_profile(fit)
print(mp)
plot_meditcv_profile(mp)
}

}
\references{
Frank, K. A. (2000). Impact of a confounding variable on a regression
coefficient. \emph{Sociological Methods & Research}, 29(2), 147--194.
}
\seealso{
\code{sensitivity_meditcv()}, \code{plot_meditcv_profile()},
\code{sensitivity_curvature()}
}
