% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sensitivity.R
\name{plot_sensitivity}
\alias{plot_sensitivity}
\title{Sensitivity Contour Plot: Joint E-value × Rho Display}
\usage{
plot_sensitivity(
  x,
  annotate_zero = TRUE,
  n_breaks = 12,
  palette = "RdYlGn",
  ...
)
}
\arguments{
\item{x}{A \code{robmedfit} object.}

\item{annotate_zero}{Logical. Draw a bold dashed zero-crossing contour?
Default \code{TRUE}. Highly recommended for applied papers.}

\item{n_breaks}{Number of fill colour breaks in the heatmap. Default \code{12}.}

\item{palette}{RColorBrewer palette name. Default \code{"RdYlGn"}.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot2} object.
}
\description{
Renders the novel bivariate robustness map unique to \strong{RobustMediate}: a 2-D
heatmap where the x-axis is the E-value (VanderWeele-style unmeasured
treatment–outcome confounding) and the y-axis is Imai's sequential-ignorability
violation parameter ρ. Contour lines show where the mediation effect crosses
zero, so readers can judge robustness to \strong{two different} sensitivity
dimensions simultaneously.

This visualisation does not exist elsewhere in the R ecosystem. The correct
interpretation is as a \emph{bivariate robustness display}, not a joint causal
model — see the package paper for theoretical justification.
}
\examples{
\dontrun{
fit <- robustmediate(X ~ Z, M ~ X + Z, Y ~ X + M + Z, data = mydata)
plot_sensitivity(fit)
plot_sensitivity(fit, annotate_zero = FALSE, palette = "PuOr")
}

}
